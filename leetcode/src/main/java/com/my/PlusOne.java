package com.my;

import java.util.ArrayList;

/**
 * 加一
 * @author shang
 */
public class PlusOne {
    public int[] plusOne(int[] digits) {
        //防止digits+1溢出了比如9999+1  10000
        int[] result = new int[digits.length+1];
        //先把digits转成int
        int length = digits.length;
        boolean isAdd = false;
        for (int i = length-1; i >= 0; i--) {
            if()
        }

    }
}
